<div class="w-full px-4 md:px-8 mx-auto py-10">
  <h2 class="text-gray-800 text-2xl lg:text-3xl font-bold text-center mb-4 md:mb-8">診療日</h2>
    <%= form_with(model: @clinic, local: true) do |f| %>
      <%= render 'form_schedule', f: f %>

        <%= f.submit @clinic.new_record? ? "登録" : "更新", class: "block bg-gray-800 hover:bg-gray-700 active:bg-gray-600 focus-visible:ring ring-gray-300 text-white text-sm md:text-base font-semibold text-center rounded-lg outline-none transition duration-100 px-8 py-3" %>

        <%= link_to '戻る', :back %>
    <% end %>
    </div> 
  </div>
